const { /*List,*/ Map } = require('immutable');

export default {
    file: Map({
        uploaded: false,
        done: false,
    }),
    // app: Map({
    //   size: 200,
    //   tag: 'art',
    //   tags: List()
    // })
};
